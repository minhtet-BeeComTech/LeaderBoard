import {View} from 'react-native';
import styled from '@emotion/native';

export const StyledNetInfoContainer = styled(View)((props: any) => ({
  flex: 1,
  flexDirection: 'column',
  justifyContent: 'center',
  alignItems: 'center',
}));

export const StyledCommingSoonContainer = styled(View)((props: any) => ({
  flex: 1,
  flexDirection: 'column',
  justifyContent: 'center',
  alignItems: 'center',
}));
